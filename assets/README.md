# Assets Placeholder

This directory intentionally contains no binary files. Add your own pixel-art sprites or audio files here if desired.

To include custom graphics, place image files (e.g., PNG) in this folder and load them in `src/game/scene.ts`.
For sound effects or music, add audio files (e.g., OGG/WAV) here and update `src/core/audio.ts` and `src/game/scene.ts` accordingly.
